# Django core
Django~=5.2.12
django-environ~=0.13.0
whitenoise~=6.12.0

# Security
# Brute-force protection on sign-in (lockout after repeated failures).
django-axes~=8.3
# Content-Security-Policy header. Report-only until CSP_ENFORCE=True.
django-csp~=4.0
# Fernet encryption for the Xero OAuth tokens at rest.
# Floor is 50.0.0: everything below it carries PYSEC-2026-3552.
cryptography~=50.0

# WSGI server (Railway / any always-on host; not used by Vercel)
gunicorn~=23.0

# Database
psycopg2-binary~=2.9.11

# PDF Generation
reportlab~=4.4.10

# Task queue
celery~=5.6.2
redis~=7.4.0
django-celery-beat~=2.9.0
django-celery-results~=2.6.0

# Image handling
# Floor is 12.3.0. Pillow decodes files that people upload, so a decoder
# CVE here is reachable by anyone who can add a horse photo. 12.1.1
# carried 27 of them.
Pillow~=12.3
pillow-heif~=1.4.0  # decode iPhone HEIC uploads (converted to JPEG on save)

# Development
django-debug-toolbar~=6.2.0

# Testing
# The suite runs on Django's own test runner (`python manage.py test`).
# pytest and pytest-django were declared but never used — no imports, no
# pytest.ini, no CI step — and pytest 8.0.2 carries PYSEC-2026-1845.
# Removed rather than upgraded: an unused dependency is attack surface
# and audit noise for no benefit.

# HTMX support
django-htmx~=1.27.0

# Forms
django-crispy-forms~=2.6
crispy-tailwind~=1.0.3

# Xero API integration
requests~=2.33.0

# Field boundaries (location mapping): polygon validation, winding order,
# simplification, point-in-polygon and badge anchors, all server-side.
shapely~=2.1

# Product analytics (server-side sign in / sign out events).
# The browser half loads posthog-js from the PostHog CDN.
posthog~=7.47

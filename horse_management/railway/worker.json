{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "python manage.py restore_test; celery -A horse_management worker --loglevel=info --concurrency=2",
    "restartPolicyType": "ALWAYS"
  }
}

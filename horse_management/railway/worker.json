{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "celery -A horse_management worker --loglevel=info --concurrency=2",
    "restartPolicyType": "ALWAYS"
  }
}
